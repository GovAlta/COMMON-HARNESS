# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Coverage
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# RSA keys (development only — production injects PEMs via env vars from Key Vault)
server/keys/

# Defense-in-depth: any PEM/key/certificate, no matter where it lands
*.pem
*.key
*.p12
*.pfx

# Test + scan output
.lighthouseci/
.nyc_output/

# AI / security framework outputs — every team writes under .ai/<team>/.
# Keep this gitignored so scan output never leaks into the repo.
.ai/

# The blueteam scanner regenerates eslint.security-scan.config.mjs at run time
# with absolute paths to its own node_modules (so plugin/parser resolution
# works regardless of where the harness is checked out). Committing a stale
# copy bakes the writing dev's local paths into the repo and silently bypasses
# the scan on any other machine.
eslint.security-scan.config.mjs

# AI shadow files (auto-generated by Velocity)
__AI__*.md
